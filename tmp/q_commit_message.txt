Feat(events): Display dynamic icons on event listing page

- Updated `frontend/src/app/dashboard/events/page.tsx` to display
  the event-specific icon fetched from the backend.
- The `Event` interface was updated to include `icon?: string;`.
- The event card's left section now renders a Material Icon using
  `<span className="material-icons">...</span>`.
- It displays `event.icon` if available, otherwise defaults to 'event'.
- Added `group-hover` styling to change icon color on card hover.
- Updated `.Q/projectlog.md` to reflect this frontend enhancement.